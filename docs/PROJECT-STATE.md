# Plux — מצב הפרויקט

עדכון אחרון: 26.08.2026 (יום 9)

מסמך מסירה. נועד לאפשר לשיחה חדשה להמשיך מכאן בלי לגלות מחדש
מה נבנה, למה, ומה כבר נבדק. **קרא אותו לפני שאתה משנה קוד.**

**עדכון מהיום:** שבעת השלבים המקוריים מהמפרט סגורים — כולל אנליטיקה,
דשבורד, תנועות, ניווט וצ'אט, שהגרסה הקודמת של המסמך (מיום 4) עדיין
סימנה כ-❌ "טרם". היומן המלא של ימים 5–7 עבר ל-`docs/plux-explain.md`
(סעיפים 9.23–9.31 ו-12–14), באותו פורמט שכל יום קודם תועד בו. המסמך
הזה עודכן בהתאם: טבלת המצב, מבנה הקוד, וההחלטות החדשות.

**עדכון נוסף, מאותו יום:** שלב 8 (יעדי חיסכון) נפתח. `SavingsGoal`
מקבל CRUD מלא, חישוב סכום חודשי נדרש, ובדיקת ריאליות מול הנטו הממוצע
בפועל (commit `715cd8b`). זה גם הקוד הראשון בפרויקט שכותב ל-`Decimal`
ולא רק קורא ממנו — ראו החלטה 11. מסומן ✅ למטה: 8.1–8.3, 8.5.
עדיין לא: 8.4 (צעדים מומלצים).

**עדכון מיום 25.08:** `Budget` (תקציב חודשי לפי קטגוריה) נבנה במלואו —
מנוע `under`/`near`/`over` עם סף 80% להתראה מוקדמת, `setBudget` כ-upsert
יחיד לפי [userId, categoryId], מסך `/budget` שקורא ל-`factsFor` הקיים
ולא מחשב הוצאה בעצמו (commit `1ba0091`).

**עדכון מאותו יום:** `8.4` (צעדים מומלצים מתוך ממצאי המנוע) נבנה
— `recommendSteps` ב-`lib/savings/engine.ts`, פונקציה טהורה שמנסחת את
מה ש-`goalStatus`/`assessRealism` כבר הכריעו, בלי מקור מידע נוסף
(commit `96c7903`). **שלב 8 (תוכנית פיננסית: יעדי חיסכון + Budget)
סגור במלואו.**

**עדכון אחרון מאותו יום:** שלושה מארבעת הפערים הקטנים שנשארו נסגרו
יחד — 2.8 (commit `50c0d11`), 4.10 (commit `e0e7fbc`), 4.8
(commit `bc0ac5f`). 4.8 עבר גם בדיקה אמיתית מול ה-API עם 74 בתי עסק
אמיתיים, וחשף שני באגים אמיתיים בדרך (MAX_TOKENS קטן מדי לעברית,
ו-`process.exit(0)` שמקריס Windows כש-Anthropic SDK משאיר socket
פתוח) — שניהם תוקנו ומתועדים בקומיט `bc0ac5f`. **מהמפרט המקורי נשאר
פער אחד בלבד: 3.11–3.12, שוויתרנו עליו במכוון ב-26.08 (ראו למטה) — כלומר המפרט המקורי סגור במלואו.

**עדכון נוסף, אותו יום (25.08) — מעבר לשבעת השלבים המקוריים:** דף
הבית הפך למרכז שליטה מאוחד במקום מסך ניווט גרידא. שלוש תוספות לא
מהמפרט המקורי, כולן מנוסחות מעל מנועים קיימים ולא מקורות נתון
חדשים: **יתרת חשבון** (`lib/accounts`, מנסח `balanceAfter` שכבר
נשמר בכל ייבוא לאומי), **המלצות** (`lib/recommendations`, שלוש
תובנות מ-`recurring`/`budget`/`savings` הקיימים — הוראת קבע לחיסכון,
חריגת תקציב ברצף, וטיפ "כסף עומד ללא ריבית"), ו**טיקר שוק חי**
(`lib/market`, Twelve Data, SPY/QQQ/AAPL/NVDA — לא סימבולי מדד
גולמיים, שקוף על כך בתווית). לצד זה מסד עיצוב חדש לגמרי
(`app/globals.css`: כחול כצבע מותג, שמונה גווני קטגוריה מאומתים,
Heebo) שהחליף `black/white` גולמי בדשבורד/חיסכון/תקציב/צ'אט/ייבוא/
בית, ושם הבוט בממשק שונה מ"שיחה" הכללית ל-**Pluxer**. commits
`d37b5dc`, `f2ec05e`, `e49dcbe`, מתועדים בפירוט בהמשך המסמך.

**עדכון מיום 26.08 — רה-דיזיין קטגוריות MAX ותיקוני יציבות:** קטגוריית
MAX (שדה "קטגוריה" בקובץ עצמו) הפכה **ליעד סיווג ישיר** במקום ברירת
מחדל שכללי `rules.ts` פיצלו לפי שם בית עסק — החלטת מוצר מפורשת של
המשתמש, כדי שהאפליקציה תציג בדיוק את מה שכתוב בקובץ שהוא מייבא. שלוש
תוצאות: תשלומי ביט נספרים כהוצאה אמיתית (`transfers_out.bit`, לא
עוד מוחרגים כהעברה), "שונות" ממופה ל-`misc` ממשי במקום `null`, וקטגוריית
MAX 12-ית שהתגלתה בקובץ נוסף — **טיסות ותיירות** — קיבלה יעד עצמאי
(`travel`). `SNAPSHOT_VERSION` עלה ל-7. גם: מיון חיובים קרובים לפי
תאריך חיוב אמיתי במקום תאריך הזנה למסד (`sortUpcomingCharges`,
`lib/accounts/engine.ts`), תיקון כפילות טקסט בבאנר "חודש חלקי", ושני
תיקוני ESLint (`react-hooks/immutability` בדונאט הקטגוריות, משתנה לא
בשימוש ב-`dedupe-check.mts` החדש). **תקלה נפרדת שנפתרה באותו יום, לא
באג קוד:** `classify-check.mts --resync` נכשל ב-P2003 כי מזהה המשתמש
של Clerk ששימש לאורך השיחה היה שגוי בשתי אותיות — לא RLS, לא pooler.
פורט מלא ליומן ב-`docs/plux-explain.md` (סעיפים 9.39–9.42, 16),
כולל את כל עבודת יום 8 (סעיפים 9.32–9.38, 15) שלא הייתה מתועדת שם עד
עכשיו. commits `b82f79d`, `578267b`.

**עדכון נוסף, אותו יום — הפער האחרון מהמפרט המקורי נסגר בהחלטה, לא בבנייה:** 3.11–3.12 (polling ו"נסה שוב" בייבוא) עברו מ"דחוי בכוונה" ל"ויתרנו עליו" — ראו הפירוט תחת "הצעד הבא המדויק" למטה. במקומו נבנה לוח מעקב חדש ל-10 המשימות שנותרות בפועל (מתוך שלבים 7 ו-9, מעבר למפרט המקורי), כארטיפקט עם שמירה חיה (`capabilities: {artifact: {}}`) — לא רק לצפייה כמו הלוח המקורי.

---

## מה זה

אפליקציית ניהול הוצאות אישיות. המשתמש מעלה דוחות מחברת האשראי (MAX)
ומהבנק (לאומי), המערכת מפענחת אותם, מסווגת תנועות, ומציגה תמונה
פיננסית — כולל בוט שעונה על שאלות בשפה חופשית.

**סטק:** Next.js 16.3.1 (App Router) · TypeScript · Tailwind 4 ·
Prisma 7.9.1 · PostgreSQL ב-Supabase · Clerk לזהות · Vercel לדיפלוי.

**ריפו:** `github.com/deni-ing/plux` · **מקומי:** `C:\Users\danie\source\plux`

---

## מצב נוכחי — 7/7 שלבים ראשיים סגורים

הספירה הישנה ("X/86 משימות") לא עודכנה מיום 4 והתייחסה לפירוק עדין
יותר של תת-משימות שלא תועד כמסמך נפרד — לכן הוחלפה כאן במה שאפשר
לאמת ישירות מ-`git log` ומהקוד עצמו: כל שבעת השלבים סגורים, עם רשימת
פערים קטנים ומתועדים בסוף המסמך (ראו "הצעד הבא המדויק").

| שכבה | מצב |
|---|---|
| סכימה ומיגרציות | ✅ 3 מיגרציות מוחלות |
| RLS ובידוד משתמשים | ✅ מוכח בטסט, 6/6 |
| זהות (Clerk) | ✅ עובד מקומית ובייצור |
| דיפלוי (Vercel) | ✅ חי, התחברות אומתה |
| פרסר MAX (XLSX) | ✅ 5 קבצים אמיתיים, כל האימותים |
| פרסר לאומי (PDF) | ✅ 169 תנועות, 168/168 חוליות |
| ממשק פרסרים משותף | ✅ זיהוי לפי בתים |
| כתיבה למסד (`ingest`) | ✅ 169 תנועות נכתבו, dedup אומת |
| מסך העלאה + API | ✅ /import ו-POST /api/imports |
| אחסון הקובץ הגולמי | ✅ דלי פרטי + מחיקה אחרי 30 יום |
| סיווג תנועות | ✅ 98.5% מ-392 תנועות אמיתיות |
| עץ קטגוריות | ✅ 96 קטגוריות, שתי שכבות, slug יציב — MAX הוא היעד הישיר מ-26.08 |
| מנוע כללים | ✅ 23 כללי-מבנה (לאומי בלבד, לא ניחוש-שם) מ-26.08, ירד מ-90 |
| שכבת AI (סיווג) | ✅ `ClaudeClassifier` מחובר בפועל, נבדק על 74 בתי עסק אמיתיים (4.8) |
| תיקון ידני → כלל | ✅ עובד, EXACT בעדיפות 10 |
| CI (GitHub Actions) | ✅ ירוק. ריצה #20 היא האחרונה, על commit `8df1808` |
| בדיקת חוקיוּת (checkup) | ✅ 64 בדיקות, 0 כשלים |
| ESLint | ✅ 0 שגיאות, 0 אזהרות |
| **מנוע אנליטיקה (שלב 5)** | ✅ 116+ טסטים, snapshot עם גרסה, תחזית |
| **דשבורד + תנועות + ניווט (שלב 6)** | ✅ Server Components, מצב חי ב-URL |
| **צ'אט (שלב 7)** | ✅ שלושה כלים, הזרמה, טרנזקציה קצרה לכלי |
| שכבת AI (צ'אט) | ✅ מחובר בפועל ל-Claude, לא מדומה |
| **יעדי חיסכון (שלב 8, מלא)** | ✅ CRUD, חישוב חודשי נדרש, ריאליות, צעדים מומלצים, אזהרה |
| Budget | ✅ מנוע under/near/over, upsert, מסך /budget |
| בית — מרכז שליטה | ✅ AskBox ← Pluxer, יתרת בנק, המלצות, תוכנית |
| יתרה והמלצות (`lib/accounts`, `lib/recommendations`) | ✅ 13 טסטים, לא מקור נתון רביעי |
| טיקר שוק חי (`lib/market`) | ✅ Twelve Data — SPY/QQQ/AAPL/NVDA |
| מסד עיצוב (טוקנים) | ✅ כחול, 8 גווני קטגוריה מאומתים, Heebo |

---

## מבנה הקוד

```
lib/db/client.ts      חיבור למסד תחת RLS. withUser() = נקודת הכניסה היחידה
lib/db/session.ts     הגשר בין Clerk למסד. withCurrentUser(), syncCurrentUser()
lib/parsers/types.ts  טיפוסים משותפים + חשבון בכסף שלם (אגורות)
lib/parsers/xlsx.ts   קורא XLSX ללא תלויות — ZIP + XML בלבד
lib/parsers/max.ts    פרסר MAX
lib/parsers/leumi.ts  פרסר לאומי. מקבל שורות טקסט, לא PDF
lib/parsers/pdf-text.ts  הקובץ היחיד שתלוי ב-unpdf
lib/parsers/index.ts  הממשק המשותף + parseStatement()
lib/import/ingest.ts  כתיבת תוצאות פענוח למסד
lib/storage/statements.ts  שמירת הדוח הגולמי. service_role, שרת בלבד
lib/db/maintenance.ts      החריג היחיד ל-RLS. עבודות מערכת בלבד
lib/categories/tree.ts     עץ הקטגוריות. 85 slugs, שתי שכבות, שלושה kinds
lib/categories/ensure.ts   יצירת הקטגוריות למשתמש. 4 שאילתות לכל היותר
lib/classify/engine.ts     ההחלטה עצמה. טהור — בלי מסד, בלי Next
lib/classify/rules.ts      23 עובדות מבניות של לאומי (עמלה/העברה/מזומן). מ-26.08 לא מפצל MAX
lib/classify/provider-max.ts  12 קטגוריות MAX → slugs, היעד הישיר (מ-26.08). "שונות"→misc, לא null
lib/classify/store.ts      השכבה שנוגעת במסד + coverage()
lib/classify/run.ts        שלוש טרנזקציות קצרות, לא אחת ארוכה
lib/classify/user.ts       תיקון ידני. USER הוא סוף פסוק
lib/classify/ai/           ממשק מסווג, פרומפט טהור, מימוש מדומה
lib/storage/paths.ts       מוסכמת storagePath. שלוש הצורות במקום אחד
scripts/checkup.mts        בדיקת חוקיוּת: אינווריאנטות בנתונים עצמם
scripts/fix-storage-paths.mts  המרת סימונים ישנים. חד-פעמי
.github/workflows/ci.yml   ESLint → בנייה → טיפוסים → סריקת סודות
app/import/page.tsx        מסך ההעלאה
app/api/imports/route.ts   קליטת דוח
app/api/cron/purge-statements/route.ts  מחיקה אחרי 30 יום
proxy.ts              Clerk middleware
scripts/*.mts         בדיקות והרצות ידניות

— שלב 5, מנוע אנליטיקה —
lib/analytics/money.ts     חשבון בכסף שלם (אגורות) + formatILS
lib/analytics/period.ts    גבולות חודש. basis: charged / booked
lib/analytics/spend.ts     הפירוק המרכזי: קטגוריות, השוואה, כיסוי כפול
lib/analytics/fees.ts      זיהוי עמלות
lib/analytics/recurring.ts חיובים חוזרים. annualized רק ל-subscription
lib/analytics/forecast.ts  תחזית לסוף חודש: floor/expected/ceiling + הנחות
lib/analytics/snapshot.ts  טיפוס AnalyticsSnapshot. SNAPSHOT_VERSION=2
lib/analytics/recompute.ts חישוב ושמירת סנפשוטים חסרים
lib/analytics/load.ts      טעינת תנועות לטווח
scripts/spend.mts          דוח הוצאות משורת הפקודה. קודם לדשבורד
scripts/snapshot.mts       חישוב/הצגת סנפשוטים
scripts/analytics-check.mts  בדיקת חוקיוּת + טריות סנפשוט מול הנתונים

— שלב 6, דשבורד/תנועות/ניווט —
app/dashboard/page.tsx     Server Component. חודש נבחר חי ב-URL
lib/analytics/facts.ts     AnalyticsSnapshot → מבנה מוכן לדשבורד
components/dashboard/parts.tsx  כרטיסים, כיסוי כפול, תחזית עם הנחות
app/transactions/page.tsx  ממתין-להכרעה בראש, סינון כטופס GET
app/transactions/actions.ts  סיווג מחדש. מחשב snapshot מחדש מחוץ לטרנזקציה
lib/txns/browse.ts         שאילתת התנועות למסך, ממוינת לפי ערך
components/nav.tsx         ניווט. רכיב עצמאי, לא חלק מה-layout

— שלב 7, צ'אט —
lib/chat/tools.ts          שלושה כלים: getMonthlyReport/findTransactions/listAvailableMonths
lib/chat/present.ts        factsForAi: אגורות→שקלים + אזהרת annualized שוב
lib/chat/client.ts         runChat/streamChat. MAX_TOOL_ROUNDS=6, טרנזקציה קצרה לכלי
components/chat/screen.tsx "use client" היחיד בפרויקט. busyRef נגד שליחה כפולה
app/api/chat/route.ts      ReadableStream. שגיאה ידועה = JSON, שגיאה באמצע = לתוך הזרם

— שלב 8, יעדי חיסכון —
lib/savings/engine.ts      פונקציות טהורות: התקדמות, סכום חודשי נדרש, ריאליות, צעדים מומלצים (8.4)
lib/savings/store.ts       שכבת המסד. withUser, כמו lib/classify/store.ts
app/savings/page.tsx       מסך. Server Component בלבד
app/savings/actions.ts     יצירה/הפקדה/מחיקה. Server Actions
components/savings/parts.tsx  GoalCard, NewGoalForm
scripts/savings-check.mts  דוח יעדים משורת הפקודה

— שלב 8, תקציב —
lib/budget/engine.ts       פונקציות טהורות: budgetStatus (under/near/over), budgetPct
lib/budget/store.ts        שכבת המסד. setBudget הוא upsert יחיד לפי [userId, categoryId]
app/budget/page.tsx        מסך. Server Component בלבד
app/budget/actions.ts      setBudgetAction/deleteBudgetAction. Server Actions
components/budget/parts.tsx  BudgetRow, NewBudgetForm
scripts/budget-check.mts   דוח תקציב משורת הפקודה

— בית, יתרה, המלצות, טיקר —
lib/accounts/engine.ts     פונקציות טהורות: summarizeBalance, sortUpcomingCharges (מ-26.08, לפי תאריך חיוב)
lib/accounts/store.ts      שכבת המסד. bankBalance — null כשאין חשבון בנק
lib/recommendations/engine.ts  שלוש תובנות מנוסחות: הוראת קבע, חריגת תקציב, כסף עומד
lib/recommendations/store.ts   שכבת המסד. קורא ל-recurring/budget/savings הקיימים
lib/market/index.ts        טיקר שוק. Twelve Data, ETF proxies ל-S&P/Nasdaq
scripts/dedupe-check.mts   ביקורת כפילויות אמיתיות (dedupHash שונה לתנועה זהה-לכאורה). מ-26.08
components/home/parts.tsx  BalanceCard/ForecastCard/GoalProgress/PlanCard/PendingBanner
components/home/ask-box.tsx     תיבת שאלה חופשית ← /chat?q=
components/home/market-ticker.tsx  כרטיס הטיקר
lib/categories/palette.ts  slug → צבע קטגוריאלי (8 slots) + אייקון
components/categories/icon.tsx  אוסף אייקוני SVG. מפתחות רק מ-CATEGORY_TREE
components/dashboard/category-donut.tsx  CategoryDonut, TopCategoryTiles
```

---

## החלטות שאסור לבטל בלי להבין

### 1. `withUser` ו-`SET LOCAL`

כל גישה לנתוני משתמש עוברת ב-`withUser(userId, work)`, שפותח טרנזקציה
ומריץ `set_config('app.current_user_id', ..., true)`.

**ה-`true` הוא `SET LOCAL`, לא `SET` רגיל.** אנחנו מאחורי pooler
ב-transaction mode: חיבור חוזר לבריכה בסוף הבקשה. `SET` רגיל היה נשאר
על החיבור, והבקשה הבאה הייתה יורשת את זהות המשתמש הקודם ורואה את
הנתונים שלו. באג שלא מתגלה בפיתוח, כי בפיתוח יש משתמש אחד.

### 2. שני תפקידי מסד, ולא אחד

| משתנה | תפקיד | פורט | למה |
|---|---|---|---|
| `DATABASE_URL` | `plux_app` | 6543 | האפליקציה. **בלי `BYPASSRLS`** |
| `DIRECT_URL` | `postgres` | 5432 | מיגרציות בלבד |

**הסיפור:** הפעלנו RLS, נראה מצוין, וטסט הבידוד נכשל 5/6. הסיבה —
משתמש `postgres` של Supabase נושא את התכונה `BYPASSRLS` ומתעלם מכל
מדיניות, **בשקט**. `FORCE ROW LEVEL SECURITY` מטפל בבעל הטבלה, לא
בתכונה הזו. שני מנגנוני עקיפה שונים.

**אם `DATABASE_URL` יחזור להצביע ל-`postgres` — כל ההגנה מתבטלת ושום
דבר לא יצעק.** `scripts/doctor.mts` בודק את זה.

### 3. `countsAsSpending` — החיוב הכפול

חיוב MAX מופיע פעמיים במערכת: כשורות בודדות בקובץ MAX, וכשורה מרוכזת
אחת בדף הבנק. **אומת במספרים:** סכום לשונית "עסקאות במועד החיוב"
בקובץ 08/2026 הוא 6,033.14 — וזהו בדיוק הסכום של שורת `מקס איט פיננ-י`
מ-10.08.2026 בדף לאומי.

בדף החשבון **86 מתוך 169 השורות הן חיובי אשראי**, בסך 95,445 ש״ח.
לכן הן מסומנות `CARD_SETTLEMENT` ו-`countsAsSpending=false`: ההוצאה
נספרת מצד MAX, שם יש בית עסק וקטגוריה.

**עסקאות חו״ל מתנהגות הפוך** — כל אחת מחויבת בבנק בנפרד תוך ימים,
ולא נצברת לחיוב החודשי. אומת: 488.46 ב-12.07, 13.70 ב-21.07.
שני חיובי Steam של 21.49 מאותו יום הופיעו בבנק כשורה אחת של 42.98.

### 4. `dedupHash` בנוי רק משדות יציבים

`bookedAt + merchantRaw + originalAmount + originalCurrency + cardLast4`.

**בלי** סכום חיוב, **בלי** סטטוס, **בלי** תאריך חיוב — כי בדיוק אלה
משתנים כשעסקה עוברת מ"טרם נקלטה" ל"נקלטה". אחרת אותה עסקה נכנסת פעמיים.

`occurrence` מטפל בכפילויות אמיתיות: נצפו 4 חיובים זהים של 6.00 באותו
יום ואותו בית עסק. כולם עסקאות אמיתיות ואסור לאבד אותן.

בלאומי ה-hash כולל את **היתרה המצטברת**, כי אין שם מזהה תנועה ושתי
תנועות זהות באותו יום נבדלות רק בה.

### 5. אימות מול הצהרת הספק — לא מול עצמנו

- **MAX:** שורת "סך הכל" בתחתית כל לשונית
- **לאומי:** שרשרת היתרות. `יתרה[i] − יתרה[i+1] = הסכום החתום של שורה i`

**זה תפס שלושה באגים שאף אחד מהם לא זרק שגיאה:** תא ריק ב-XML שהזיז
עמודה שלמה, `<v xml:space="preserve">` שהעלים שער המרה, וערבוב של
סכומי חיוב עם סכומי עסקאות ממתינות.

בלאומי הסימן (חובה/זכות) **נגזר מהיתרות ולא ממיקום עמודה** — מיקומי
העמודות בפלט הטקסט אינם עקביים. הפרסר גם לא מניח באיזה סדר מגיעים
שני הסכומים: הוא מנסה את שתי האפשרויות ובוחר את זו שמסבירה את השרשרת.

### 6. הפרדת משתמשים באחסון — במבנה נתיב, לא ב-RLS

Storage של Supabase נשען על RLS שמצפה לזהות של Supabase Auth. אנחנו על
Clerk, ולכן אין התאמה. הפתרון: **השרת בלבד ניגש לאחסון**, עם מפתח
`service_role`, ואוכף את ההפרדה בנתיב — `<userId>/<jobId>/<שם>`.

הדלי פרטי ואין עליו מדיניות (`POLICIES: 0`) — **וזה מכוון**, כי אף אחד
לא ניגש אליו ישירות. `SUPABASE_SERVICE_ROLE_KEY` נקרא בתוך פונקציה ולא
ברמת המודול, כך שייבוא בטעות מקוד לקוח נכשל בבנייה במקום להדליף.

### 7. `lib/db/maintenance.ts` — החריג היחיד ל-RLS

עבודת המחיקה התקופתית רצה בלי משתמש מחובר וחייבת לראות את כל המשתמשים,
ולכן היא מתחברת ב-`DIRECT_URL` (תפקיד `postgres`, עם `BYPASSRLS`).

שלוש הגבלות שהופכות את זה למקובל: מיובא רק מנתיבי cron המוגנים בסוד
משותף, נוגע רק במטא-דאטה של קבצים, ואין בו שום שאילתה על תנועות.
**אם מישהו מוסיף שם `findMany` על נתונים פיננסיים — זה הרגע לעצור.**

### 8. הפרסרים לא יודעים שיש מסד

הטיפוסים ב-`lib/parsers/types.ts` הם איחודי מחרוזות, לא enums של Prisma.
`lib/db/client.ts` מקבל `userId` כפרמטר ולא שולף אותו מ-Clerk. לכן אפשר
להריץ הכל מסקריפט בלי דפדפן — וזה מה שמאפשר את כל הבדיקות הידניות.
**אל תייבא Next.js או Prisma לתוך `lib/parsers/`.**

### 9. `SNAPSHOT_VERSION` — מטמון שיודע לבטל את עצמו

`AnalyticsSnapshot` הוא מטמון על תוצאת חישוב חודש-משתמש, לא מקור אמת.
`SNAPSHOT_VERSION` עולה **רק** כששדה קיים משנה משמעות (לדוגמה: `partial`
עבר מ"אין תנועה ביום האחרון" ל"הנתונים נגמרים בתוך החודש") — לא בכל
תוספת שדה. בלי ההבחנה הזו, שינוי משמעות שקט משאיר סנפשוטים ישנים
"עונים נכון" לשאלה הישנה ובטעות גם לחדשה. `scripts/analytics-check.mts`
מוודא טריות בהשוואה **סמנטית** (מפתחות ממוינים רקורסיבית), לא
`JSON.stringify` גולמי — כי `jsonb` ב-Postgres מנרמל סדר מפתחות בעצמו,
ואותו אובייקט לעולם לא יוצא כאותה מחרוזת פעמיים.

### 10. טרנזקציה קצרה לכל קריאת כלי בצ'אט, לא אחת לכל השיחה

`runChat`/`streamChat` (`lib/chat/client.ts`) מקבלים `withUser`
(פותח-טרנזקציה), לא `db` מוכן. כל קריאת כלי פותחת טרנזקציה קצרה משלה.

**הסיבה בדיוק כמו בהחלטה 1 בהקשר חדש:** טרנזקציה אינטראקטיבית של
Prisma נסגרת אחרי חמש שניות, וסיבוב שיחה עם קריאת כלי לוקח יותר מזה —
לא כי המסד איטי, אלא כי רוב הזמן הוא המתנה לרשת ל-Claude. **טרנזקציה
לא אמורה להחזיק פתוח משהו שהיא לא שולטת במשך הזמן שלו.**

### 11. `fromAgorot` — הכיוון ההפוך, לראשונה בשלב 8

עד שלב 8 הקוד רק *קרא* `Decimal` (`toAgorot`). `SavingsGoal.target`
ו-`SavingsGoal.saved` הם השדה הראשון שהקוד גם *כותב* אליו, מטופס
משתמש. הפיתוי הטבעי הוא `(agorot / 100).toString()` — וזו בדיוק
החזרה לנקודה הצפה שכל `money.ts` נכתב כדי להימנע ממנה.

`fromAgorot` בונה את המחרוזת מחילוק ומודולו על מספר שלם (`Math.trunc`,
`%`), בלי לעבור בנקודה צפה בשום שלב. טסט ה-round-trip
(`toAgorot(fromAgorot(x)) === x` על טווח ערכים) הוא ההוכחה לכך, לא רק
בדיקה נוספת.

---

## מלכודות שכבר עלו ביוקר

**`proxy.ts` ולא `middleware.ts`.** ב-Next.js 16 השם השתנה. הקובץ הישן
לא נטען — בלי שגיאה. `auth()` פשוט מחזיר ריק.

**Clerk Core 3 הסיר את `<SignedIn>` ו-`<SignedOut>`.** הוחלפו ב-`<Show
when="signed-in">`. גרסת ה-npm (`^7.x`) לא מסגירה את דור ה-API.

**`Copy-Item` עם תיקיות יוצר קינון במקום למזג.** `Copy-Item src\* -Destination
. -Recurse` יצר `lib\lib\db\client.ts`. **השתמש ב-`robocopy src . /E /IS`.**

**מיגרציה שהוחלה היא היסטוריה.** Prisma שומרת checksum; עריכה שלה
שוברת את השרשרת ומזמינה `migrate reset`. מוסיפים מיגרציה חדשה.

**ואמת אחת שחזרה על עצמה שלוש פעמים ביום אחד:** לפני שמנתחים התנהגות,
ודא שהקוד על הדיסק הוא הקוד שנכתב. `Get-FileHash` או `Select-String`
על מילה שקיימת רק בגרסה החדשה — שתי שניות, וחוסכות סבב שלם.

**P2003 על טבלה שהמשתמש "בטוח" קיים בה, יכול להיות זהות שגויה, לא RLS.**
26.08: שגיאת מפתח זר על `categories_userId_fkey` נראתה כמו כשל RLS או
כשל pooler — אף אחד מהם לא היה זה. מזהה Clerk ששימש לאורך שיחה שלמה
היה שגוי בשתי אותיות. לפני שחושדים בשכבות המסד, `scripts\debug-user-row.mts`
(או שאילתה ישירה עם `pg.Client` על `DIRECT_URL`, עוקפת Prisma ו-RLS)
מוודא תוך דקה מי בכלל קיים בטבלת `users`.

---


**מיום 4:**

- **`migrate dev` לא תמיד מריץ `prisma generate`.** הסכימה והמסד התעדכנו,
  והקוד המשיך לראות טיפוסים ישנים. הרץ `generate` מפורש אחרי כל מיגרציה.
- **`next build` לא מריץ ESLint.** שגיאת lint אמיתית ישבה בריפו יממה,
  דרך בנייה ופריסה לייצור. רק ה-CI תפס אותה.
- **בדיקת טיפוסים חייבת לרוץ אחרי הבנייה.** Next מייצר `LayoutProps`
  ודומיו לתוך `.next/types` בזמן הבנייה. מקומית זה עובד כי `.next` קיים
  מבנייה קודמת; על מכונה נקייה זה נכשל.
- **לולאה של `await` על המסד = פסק זמן של טרנזקציה.** 71 upsert סדרתיים
  חצו את חמש השניות (`P2028`). התיקון הוא פחות סיבובים, לא פסק זמן ארוך
  יותר. מקומית שאילתה היא מיליוניות שנייה; מול אירלנד ~70ms.
- **אל תעצור `migrate dev` באמצע.** ההיסטוריה נשארת במצב שהכלי מסרב
  להתקדם ממנו, והחילוץ הוא `migrate resolve` ידני.
- **כלל טקסט רחב בעברית מסוכן.** `CONTAINS "גים"` תפס גם "מעדני דגים".
  אין גבולות מילה. עדיף ארבעה כללים צרים מאחד רחב.
- **מדד יחסי חייב להופיע לצד המדד המוחלט.** "סווגו 3 (33.3%)" נקרא כנסיגה
  בזמן שהכיסוי עלה ל-98.5%. המכנה היה "מה שנשאר", והאחוז לא אמר זאת.


**מסוף יום 4 — מה שהבדיקה המקיפה חשפה:**

- **סימון כפול לאותה משמעות.** `local:` ו-`inline:` חיו זה לצד זה יומיים,
  כי שלושה קבצים החזיקו את המוסכמה כל אחד בעצמו. הפתרון:
  `lib/storage/paths.ts`. **ערך סימון בלי מקום אחד שמגדיר אותו יסתעף.**
- **תיקון נתונים בלי לסגור את המקור הוא דחייה.** אחרי ההמרה,
  `import-file.mts` עדיין כתב `local:` — ובייבוא הבא הכל היה חוזר.
- **בדיקה שמחזיקה עותק משלה של הידע שואלת שאלה אחרת.** שלוש אזעקות שווא
  ביום אחד, כולן מאותו דפוס: grep שמצא את הבודק עצמו, `pg_class` בלי
  סינון סכימה שמצא את `auth.users` של Supabase, ותנאי "לא inline ולא
  purged" שלא ידע על צורה שלישית. התיקון בכל שלוש: לא לרכך את הבדיקה,
  אלא לתת לה ולנבדק לחלוק הגדרה אחת.
- **בדיקה מקיפה שילמה על עצמה תוך שעה** — ובעיקר כי היא הובילה לשאלה
  "ומה כתב את זה מלכתחילה".

**מיום 5–7:**

- **הגדרת "חודש חלקי" הראשונה הייתה שגויה.** "התנועה האחרונה אינה ביום
  האחרון" תפס שבעה חודשים שלמים כחלקיים. התיקון דורש לראות תנועות
  **מעבר** לחודש המוצג כדי לדעת אם הנתונים באמת נגמרים בתוכו.
- **`JSON.stringify` אינו מבחן שוויון.** בדיקת טריות סנפשוט דיווחה 13
  "אי-התאמות" עם סכומים זהים לחלוטין בשני הצדדים — כי `jsonb` מחזיר
  מפתחות בסדר משלו. השוואה חייבת להיות סמנטית, לא מחרוזתית.
- **"הפסיק להיגבות" בלי סף איכות הוא רעש.** גרסה ראשונה של זיהוי מנויים
  שהופסקו כללה חיובים בני ₪6–₪38 שמעולם לא היו מנוי — וקברה בתוך הרעש
  שכר דירה אמיתי של ₪3,000 שלא נגבה חודשיים.
- **עדכון state ב-React אינו סינכרוני.** חסימת שליחה כפולה לפי `busy`
  (state) השאירה חלון שבו Enter/קליק כפולים חופפים. `useRef` נחסם
  סינכרונית; state לא.
- **`stop_reason: "max_tokens"` הוא תשובה נקטעת, לא תשובה סופית.** קוד
  שבודק רק `stop_reason !== "tool_use"` מציג תשובה חתוכה-באמצע כאילו
  היא שלמה. חייבים לבדוק את שלושת הערכים בנפרד: `tool_use`, `end_turn`,
  `max_tokens`.
- **כלל עיצוב על ה-UI (בלי רינדור Markdown) חייב לחיות גם ב-system
  prompt.** אחרת המודל ממשיך לייצר טבלאות ‎`|...|...|` שהלקוח מציג
  כטקסט גולמי בתוך פסקה RTL.

## פקודות אימות

```powershell
npx tsx scripts\doctor.mts        # 34 בדיקות: סביבה, קבצים, מסד, פרסר, גיט
npx tsx scripts\rls-check.mts     # בידוד משתמשים — חייב 6/6
npx prisma migrate status         # חייב "up to date"
npm run build                     # לפני כל דיפלוי
```

```powershell
$files = (Get-ChildItem $HOME\Downloads\*export*.xlsx).FullName
npx tsx scripts\parse-check.mts $files tests\fixtures\max-pending.xlsx
npx tsx scripts\leumi-check.mts "<דף-חשבון>.pdf"
npx tsx scripts\import-file.mts --dry <קבצים>
```

---

## הצעד הבא המדויק

**כל שבעת השלבים המקוריים מהמפרט סגורים.** זהות ← ייבוא ← סיווג ←
אנליטיקה ← דשבורד/תנועות/ניווט ← צ'אט. אין "השלב הבא" מובנה מאליו
יותר — הצעד הבא הוא לבחור אחד מהפערים הפתוחים למטה, או תכונה חדשה
לגמרי שלא הייתה בתכנון המקורי.

**מה שאומת בשלבים 5–7, במספרים ובשמות קבצים:**

- מנוע אנליטיקה: 116+ טסטים על נתונים סינתטיים עם תשובה ידועה מראש
  (`money`, `period`, `spend`, `fees`, `recurring`, `forecast`, `snapshot`)
- `SNAPSHOT_VERSION=2`, מאומת מול הנתונים האמיתיים ב-`analytics-check.mts`
- דשבורד ותנועות: Server Components בלבד, מצב (חודש נבחר, סינון) חי
  בכתובת ה-URL ולא ב-state
- צ'אט: שלושה כלים, הזרמת טקסט ל-`/chat`, טרנזקציה קצרה נפרדת לכל
  קריאת כלי, `SYSTEM_PROMPT` שאוסר במפורש המצאת מספרים וטבלאות טווח-רחב
- שני תיקוני צ'אט מהיום (שליחה כפולה + תשובה נקטעת שהוצגה כשלמה):
  11/11 טסטים, `npm run build`, ו-`eslint` ירוקים אצלך; CI ירוק
  (commit `8df1808`, ריצה #20)

**פתוחים וקטנים — כולם אומתו בקוד, לא מוערכים:**

~~**2.8** — כלל ESLint שימנע מ-`lib/` לייבא מ-Next.js.~~ **נסגר.**
  `eslint.config.mjs` אוכף `no-restricted-imports` על `lib/**/*.{ts,tsx}`
  בפועל, לא רק כאמנה (commit `50c0d11`)
~~**3.11–3.12** — polling ו"נסה שוב".~~ **ויתרנו עליו במכוון (26.08),
  לא נדחה יותר.** לא שתי משימות קטנות — דורש הפיכת הייבוא לאסינכרוני,
  שינוי ארכיטקטורה של ממש. בפרויקט למשתמש יחיד, עם מסלול סינכרוני
  שכבר עובד מצוין ומהיר, המורכבות לא הצדיקה את עצמה. ניתן לחזור לזה
  אם התנאים ישתנו (הרבה משתמשים, קבצים כבדים). **זה סוגר את המפרט
  המקורי במלואו — אין יותר אף פער פתוח ממנו**, רק המשך אופציונלי
  (ראו `docs/plux-explain.md` § "הספרינט האחרון" / הארטיפקט המפורסם).
- ~~**4.10** — `categorySource = "RULE"` איחד כלל-על-שם עם מיפוי-לפי-
  סוג-תנועה.~~ **נסגר.** ערך `TXN_KIND` נפרד ב-enum (מיגרציה), `RULE`
  נשאר רק להתאמת שם. סדר ההכרעה (rule → kind → provider → none) לא
  השתנה (commit `e0e7fbc`)
- ~~**4.8** — חיבור ספק AI אמיתי לשכבת הסיווג.~~ **נסגר.**
  `ClaudeClassifier` ב-`lib/classify/ai/claude.ts`, נבחר דרך
  `PLUX_AI_PROVIDER=claude`. נבדק בפועל על 74 בתי עסק אמיתיים — 14/74
  כיסוי ב-100% דיוק (מדיניות שמרנית בכוונה, ראו תיעוד `ai-eval.mts`).
  חשף ותיקן שני באגים אמיתיים ב-Windows/עברית (commit `bc0ac5f`)

~~**4.7** — מסך הכרעה בממשק~~ **נסגר בשלב 6.2.** סקציית "ממתין להכרעה"
במסך התנועות (ובדף הבית) מחליפה את מה שקודם היה רק `decide.mts`.

**מעבר למפרט המקורי, אותו יום — לא "פערים", תוספות חדשות:**

- ✅ **יתרה והמלצות** (commit `d37b5dc`) — `lib/accounts` מנסח
  `balanceAfter` קיים; `lib/recommendations` מנסח שלוש תובנות
  מ-`recurring`/`budget`/`savings` הקיימים בלי לחשב דבר בעצמו. 13
  טסטים חדשים
- ✅ **מסד עיצוב + מעבר טוקנים** (commit `f2ec05e`) — כחול כצבע
  מותג, שמונה גווני קטגוריה מאומתים (dataviz: עוגני גוון קבועים,
  CVD delta-E≥8), Heebo. דשבורד/חיסכון/תקציב/צ'אט/ייבוא עברו מ-
  `black/white` גולמי לטוקנים סמנטיים
- ✅ **מרכז שליטה בדף הבית + טיקר שוק + Pluxer** (commit `e49dcbe`) —
  AskBox מפנה ל-`/chat?q=`, יתרה/המלצות/תוכנית מוצגות בדף הבית,
  טיקר SPY/QQQ/AAPL/NVDA חי דרך Twelve Data (Stooq ננטש — anti-bot
  challenge, לא CSV), ושם הבוט בממשק שונה ל-Pluxer

**שלב 8 (תוכנית פיננסית) — נסגר במלואו ב-25.08:**

- ✅ **8.1–8.3, 8.5** — CRUD מלא (`app/savings`), חישוב סכום חודשי
  נדרש ובדיקת ריאליות מול הנטו הממוצע בפועל (`lib/savings/engine.ts`),
  מד התקדמות, אזהרת "לא ייעוץ פיננסי". אומת ידנית מול נתונים אמיתיים
  דרך `savings-check.mts` (נטו חודשי ממוצע ₪11,354.50 חושב נכון)
- ✅ **8.4** — צעדים מומלצים מתוך ממצאי המנוע (commit `96c7903`).
  `recommendSteps` מנסחת בעברית את מה ש-`goalStatus`/`assessRealism`
  כבר הכריעו — לא שאילתה או מקור מידע נוסף. 8 טסטים חדשים
- ✅ **`Budget`** (commit `1ba0091`) — תקציב חודשי לפי קטגוריה. מנוע
  טהור (`lib/budget/engine.ts`) עם סף 80% להתראה מוקדמת, `setBudget`
  הוא upsert יחיד לפי [userId, categoryId] (לא שני מסלולים ליצירה/
  עדכון), מסך `/budget` שקורא ל-`factsFor` הקיים ולא מחשב הוצאה
  בעצמו. 9 טסטים חדשים. (חיובים חוזרים כבר מזוהים דרך `recurring.ts`
  בלי טבלת `Subscription` נפרדת — זו בחירה מודעת, לא פער.)

**הערות תפעוליות:**

- סיסמת `plux_app` הוחלפה ב-20.08. אם `doctor` נכשל על חיבור — ודא
  ש-`DATABASE_URL` עודכן גם ב-`.env` וגם ב-Vercel
- משתני סביבה: `DATABASE_URL`, `DIRECT_URL`, שני מפתחות Clerk,
  `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `CRON_SECRET`.
  אופציונלי: `PLUX_AI_PROVIDER` (`none` כברירת מחדל, `mock` לבדיקות),
  `TWELVE_DATA_API_KEY` (טיקר השוק בדף הבית — בלעדיו הטיקר פשוט לא
  מופיע, לא שגיאה)
- ה-cron רשום ב-Vercel → Settings → Cron Jobs. `vercel.json` בריפו אינו
  אותו דבר כמו משימה רשומה במערכת
- **אחרי כל מיגרציה: `npx prisma generate` מפורש.** `migrate dev` לא תמיד
  מריץ אותו, והקוד ימשיך לראות טיפוסים ישנים
- **לפני כל דחיפה: `npm run build` וגם `npx eslint .`.** הבנייה אינה
  מריצה linter

## פקודות שימושיות — סיווג

```powershell
# הרצה יבשה. לא כותבת כלום
npx tsx scripts\classify-check.mts --user <id>

# כותב. --force מסווג הכל מחדש, --resync בונה קטגוריות וכללים מחדש
npx tsx scripts\classify-check.mts --user <id> --write

# מה ממתין להכרעת המשתמש, לפי סכום
npx tsx scripts\decide.mts --user <id>
npx tsx scripts\decide.mts --user <id> --slugs
npx tsx scripts\decide.mts --user <id> --merchant "<שם>" --slug <slug>

# מדידת מסווג מול הכללים
$env:PLUX_AI_PROVIDER="mock"
npx tsx scripts\ai-eval.mts --user <id>
```

## פקודות שימושיות — אנליטיקה וצ'אט

```powershell
# דוח הוצאות. ברירת מחדל: החודש האחרון שיש בו נתונים
npx tsx scripts\spend.mts --user <id>
npx tsx scripts\spend.mts --user <id> --month 2026-08 --basis charged

# חישוב/הצגת סנפשוטים. --force מחשב הכל מחדש (הכפתור שמבטל את המטמון)
npx tsx scripts\snapshot.mts --user <id>
npx tsx scripts\snapshot.mts --user <id> --show 2026-08

# בדיקת חוקיוּת שלב 5 — טריות הסנפשוט מול הנתונים האמיתיים
npx tsx scripts\analytics-check.mts --user <id>

# צלילה לשורות עצמן, לא סיכום
npx tsx scripts\txns.mts --user <id> --merchant "<שם>"
npx tsx scripts\txns.mts --user <id> --slug housing.rent --json

# העברת קטגוריה שלמה. שני slugs באנגלית בלבד — עברית לא שורדת PowerShell 5
npx tsx scripts\reclassify.mts --user <id> --from <slug> --to <slug> --write
```

לפירוט מלא של כל דגל, ראו `docs/plux-commands.md` סעיף 8.

## הבדיקה המקיפה

שתי בדיקות משלימות, ושתיהן כדאי להריץ לפני כל דחיפה:

```powershell
npx tsx scripts\doctor.mts
npx tsx scripts\checkup.mts --user <id>
```

`doctor` שואל **"האם הפרויקט מותקן נכון"** — סביבה, קבצים, חיבור, גיט.

`checkup` שואל **"האם מה שיושב במסד עקבי עם ההחלטות"** — 64 בדיקות
בשמונה חלקים: הגדרות בקוד, קבצים, RLS ובידוד, נתוני המשתמש,
אינווריאנטות, סיווג, אחסון, וסודות.

ההבדל מהותי: פרויקט יכול לעבור את `doctor` במאה אחוז ועדיין להחזיק
תנועה שמסומנת כהעברה ונספרת כהוצאה — באג שלא ייראה בשום מסך עד שמישהו
ישאל למה הסכום החודשי מוזר.

**האינווריאנטות ש-checkup אוכף:**

- `countsAsSpending` חייב לנבוע מה-`kind` של הקטגוריה
- חיוב כרטיס מרוכז לעולם אינו נספר כהוצאה (אחרת ספירה כפולה)
- כל כלל מצביע לקטגוריה קיימת ושל אותו משתמש
- תנועה מסומנת `USER` תמיד נושאת קטגוריה — הפרה = תיקון ידני שנדרס
- כל נתיב אחסון מתחיל ב-`userId` או שהוא סימון מוכר
- שאילתה בלי זהות, ושאילתה בזהות זרה, מחזירות אפס
