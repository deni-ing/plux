/**
 * 4.2 — הקטגוריה של MAX כנקודת פתיחה.
 *
 * MAX מחזירה קטגוריה לכל תנועה בעמודה "קטגוריה". זה מידע חינם ולא כדאי
 * לוותר עליו — אבל גם לא כדאי לסמוך עליו יותר מדי, ושווה להסביר למה.
 *
 * אלה 11 הערכים שהופיעו בפועל בחמישה חודשי נתונים אמיתיים, ולידם מה שהם
 * באמת מכילים:
 *
 *   מזון וצריכה (52)        סופרמרקטים, מינימרקטים, מעדניות. מדויק.
 *   פנאי, בידור וספורט (14) מערבב מנוי Apple, קניית משחק ב-Steam, מנוי
 *                           TradingView, כרטיס קולנוע, וחדר כושר. חמישה
 *                           דברים שונים לגמרי תחת שם אחד.
 *   מסעדות, קפה וברים (8)   מדויק.
 *   העברת כספים (6)         BIT. זו לא הוצאה בכלל אלא העברה.
 *   אופנה (5)               מדויק.
 *   דלק, חשמל וגז (4)       בנתונים שלנו כל ארבע היו תחנות דלק. אבל השם
 *                           מכיל גם חברת חשמל וגם ספק גז — שהם דיור, לא
 *                           תחבורה. המיפוי כאן בוחר דלק, וכללי בתי העסק
 *                           מתקנים את חברות התשתית.
 *   עיצוב הבית (3)          כלל גם AliExpress, שמוכר הכל.
 *   רפואה ובתי מרקחת (2)    מדויק.
 *   עירייה וממשלה (2)       אגרות ומוסדות ציבור.
 *   חשמל ומחשבים (2)        חנות מחשבים. השם מטעה — זו לא חשמל של הבית.
 *   שונות (1)               MAX אומרת "לא יודעת".
 *
 * המסקנה שמעצבת את הסדר במנוע: הקטגוריה של הספק היא ברירת מחדל סבירה,
 * ולכן היא אחרונה בתור. כלל על שם בית עסק תמיד גובר עליה.
 */

/** קטגוריית MAX → slug אצלנו. null = מכוון: לא לסווג. */
export const MAX_CATEGORY_MAP: Record<string, string | null> = {
  "מזון וצריכה": "food.groceries",
  "מסעדות, קפה וברים": "food.restaurants",
  אופנה: "shopping.clothing",
  "עיצוב הבית": "shopping.home",
  "חשמל ומחשבים": "shopping.electronics",
  "רפואה ובתי מרקחת": "health.pharmacy",
  "עירייה וממשלה": "financial.taxes",
  "דלק, חשמל וגז": "transport.fuel",
  "פנאי, בידור וספורט": "leisure.other",
  "העברת כספים": "transfer.p2p",

  // << "שונות" הוא MAX אומרת שהיא לא יודעת. לתרגם את זה לקטגוריה בשם
  //    "שונות" אצלנו זה להסוות אי-ידיעה כידיעה. עדיף להשאיר לא מסווג
  //    ולתת ל-AI בשלב 4.4 להתמודד — שם לפחות יהיה לזה ציון ביטחון.
  שונות: null,
};

/**
 * קטגוריות שהמיפוי שלהן גס: הן נכונות "בערך" ודורשות כלל כדי להיות מדויקות.
 * לא משפיע על הריצה — משמש רק בדוח הכיסוי, כדי שנדע כמה מהסיווג נשען על
 * ניחוש מושכל ולא על החלטה ודאית.
 */
export const MAX_COARSE_CATEGORIES = new Set([
  "פנאי, בידור וספורט",
  "דלק, חשמל וגז",
  "עיצוב הבית",
  "חשמל ומחשבים",
]);

/** נרמול קל: MAX לא עקבית ברווחים בקצוות. */
export function mapMaxCategory(raw: string | null | undefined): {
  slug: string | null;
  known: boolean;
  coarse: boolean;
} {
  const key = (raw ?? "").trim();
  if (!key) return { slug: null, known: false, coarse: false };

  const known = Object.prototype.hasOwnProperty.call(MAX_CATEGORY_MAP, key);
  return {
    slug: known ? MAX_CATEGORY_MAP[key] : null,
    known,
    coarse: MAX_COARSE_CATEGORIES.has(key),
  };
}
